<!DOCTYPE html>
<link rel="stylesheet" href="/styles.css">
<html>
  <head>
    <title>Note app</title>
  </head>
  <body>

    <main>

        <form action="createNote" method="post">
            <label for="title">Title:</label>
            <input type="text" id="title" name="title"><br>
            
            <label for="content">Content:</label>
            <textarea id="content" name="content"></textarea><br>
            
            <button type="submit">Create</button>
          </form>
    </main>
    <ul>
        <% for (let i = 0; i < listOfNotes.length; i++) {%>
        <li>
            <h2><%= listOfNotes[i].title%></h2>
            <p><%= listOfNotes[i].contents%></p>

            <form action="/deleteNote" method="post" id="deleteBtn">
                <input type="hidden" name="id" value="<%= listOfNotes[i].id %>">
                <button type="submit">Delete</button>
              </form>
        </li>
        <% } %>
    </ul>

    <footer>
      <p>Copyright © 2021 Haley Dawe</p>
    </footer>
  </body>
</html>